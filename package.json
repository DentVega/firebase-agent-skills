{
  "name": "firebase-agent-skills",
  "version": "0.2.0",
  "private": true,
  "description": "Community Firebase agent skills for AI coding assistants.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "check:snippets": "node scripts/check-snippets.mjs",
    "check:all": "npm run validate && npm run check:snippets"
  },
  "devDependencies": {
    "firebase": "^11.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "js-yaml": "^4.1.0",
    "typescript": "^5.4.0"
  }
}
